# babeld-intro

This document (in French) is an introduction to the babel meshed network routing protocol.


## Getting the PDF
### Compile-time dependencies

- python
- wkhtmltopdf
- graphviz

### Actual compiling

Simply clone the repo, install the dependencies, and run `make`.

The output file will be in `_build/babeld-intro.pdf`
